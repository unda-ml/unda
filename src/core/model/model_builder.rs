///Trait that allows for behavior like constructing layers
///Will be implemted both as a utility function that will just return 
///NodeIdentifiers, but will also be internally implemented for the Model
///struct so we can create models by doing `model.dense(n);`
pub trait ModelBuilder {

}
